spring:
  datasource:
    url: jdbc:tc:postgresql:16-alpine:///testdb
  flyway:
    enabled: true
  jpa:
    hibernate:
      ddl-auto: validate
messaging:
  enabled: false
logging:
  level:
    root: WARN
    com.customer.api: INFO

